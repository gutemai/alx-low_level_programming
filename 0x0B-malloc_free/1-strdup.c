#include "main.h"
/**
 * _strdup - function returns a pointer to a new string which is a duplicate of the str 
 * Memory for the new string is obtained with malloc, and can be freed with free.
 *@str:copied str
 *
 *Return:ptr to dup str or NULL if insufficent memory
 */
char *_strdup(char *str)
{
	
}
